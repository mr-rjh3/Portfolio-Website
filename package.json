{
  "name": "myproject",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy":"npm run build && git add dist -f && git commit -m'deploy website'  && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "vite": "^3.0.7"
  },
  "dependencies": {
    "vue": "^3.2.37"
  }
}
